APPROVAL_ORIGIN=https://your-approval-domain.example
# Server-side secret ONLY. Fine-grained PAT scoped to this repository: contents
# read, checks read, pull requests read, issues write, variables read. Never GITHUB_TOKEN
# from Actions: comments made by that token do not trigger issue_comment workflows.
GITHUB_RELAY_TOKEN=
